#pragma once

// Содержимое карты, вшитое в исполняемый файл, или nullptr, если такой карты
// нет. Используется, когда файл на диске недоступен — например, внутри APK,
// где каталога data/maps не существует.
const char* embedded_map(const char* id);
